# Azure OpenAI Configuration
# Replace with your actual Azure OpenAI endpoint
AZURE_OPENAI_ENDPOINT=""

# Replace with your model deployment name (e.g., "gpt-35-turbo", or your "o3-mini" deployment if using o3-mini specific features)
AZURE_OPENAI_DEPLOYMENT_NAME=""

# Optional: Specify API version, defaults in code if not set (e.g., "2024-02-15-preview")
# AZURE_OPENAI_API_VERSION=""

# Azure AD Authentication (if not using Azure CLI login for local dev or Managed Identity on Azure)
# If using a Service Principal, uncomment and fill these:
# AZURE_CLIENT_ID=""
# AZURE_TENANT_ID=""
# AZURE_CLIENT_SECRET=""

# NCBI Configuration for PubMed Access
# Replace with your actual email address for respectful API use
NCBI_EMAIL="your_email@example.com"

# Note: If AZURE_OPENAI_DEPLOYMENT_NAME includes "o3-mini", specialized parameters 
# (reasoning_effort: high, max_completion_tokens: 5000, developer role for system message) 
# will be automatically applied by the application.
# No separate variable is needed to enable this if the deployment name convention is followed.
